function greet(time) {
  var greet;

  if (time < 12)
      greet = 'Good Morning';
  else if (time >= 12 && hrs <= 17)
      greet = 'Good Afternoon';
  else if (time >= 17 && hrs <= 24)
      greet = 'Good Evening';
}
